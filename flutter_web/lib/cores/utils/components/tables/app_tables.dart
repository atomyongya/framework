import 'package:flutter/material.dart';

class AppTables extends StatelessWidget {
  const AppTables({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
